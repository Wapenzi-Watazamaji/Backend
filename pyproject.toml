[project]
name = "binticare"
version = "0.1.0"
description = "A Python project generated by Python Initializr"
readme = "README.md"
requires-python = ">=3.12"
license = {text = "MIT"}
dependencies = [
    "fastapi",
]

[project.scripts]
binticare = "binticare:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

